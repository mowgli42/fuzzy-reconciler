{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd frontend && npm ci && npm run build && rm -rf ../public ../src/fuzzy_reconciler/web && mkdir -p ../public ../src/fuzzy_reconciler/web && cp -R dist/* ../public/ && cp -R dist/* ../src/fuzzy_reconciler/web/",
  "installCommand": "pip install . && cd frontend && npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 60,
      "memory": 1024,
      "includeFiles": "{fixtures/**,src/fuzzy_reconciler/**}"
    }
  }
}
