# Test Report: Import & Compare Robustness (API)

**Date:** 2026-07-20  
**Branch:** `cursor/api-import-robustness-33a2`  
**Scope:** All validation via HTTP API (`/ingest`, `/compare`, `/compare/demo`, `/demo/ingest-preview`)  
**Result:** **29 passed** (`pytest -q`)

## Sample data

Generated by `scripts/generate_import_samples.py` into `fixtures/imports/`:

| File | Format | Role |
|------|--------|------|
| `source_a_alias.csv` | CSV | Heterogeneous headers (`POI_Name`, `latitude`, …) + 1 bad-geo row |
| `source_b_ops.tsv` | TSV | Ops headers (`label`, `lat_dd`, `lng`, `as_of`, `asset_type`) |
| `source_a_standard.json` / `source_b_standard.json` | JSON arrays | Canonical schema |
| `source_a.jsonl` / `source_b.jsonl` | JSONL | Line-delimited entities |
| `source_b.geojson` | GeoJSON | Point FeatureCollection |
| `source_a_bom.csv` | CSV UTF-8 BOM | BOM + attrs for spatial scoring |
| `empty.csv` / `header_only.csv` / `bad.json` | negatives | Empty / header-only / malformed |
| `manifest.json` | metadata | Expected pair classes + compare config |

Controlled overlaps in every positive pair of lists:

| Pair | Expected classification | Notes |
|------|-------------------------|-------|
| Harbor Relay North ↔ Harbor Relay North | `exact_match` or `strong_fuzzy_match` | ~few meters drift |
| Cell Site Alpha-7 ↔ Cell Site Alpha-7 | `temporal_variant` | ~17 day Δ, high name/geo/attr |
| North Tower Array ↔ Site NT-07 | `spatial_proximity_candidate` | ~180 m, low name, high attr |

Compare config (from manifest): `max_geo=350`, `date_tolerance=30`, facility-loose-style weights.

## What was tested

### Ingest formats (`TestIngestFormats`)

| Test | Expected |
|------|----------|
| CSV alias headers | format=`csv`, 7 rows, 6 geo-valid, mapping `POI_Name`→name, preview ≤15 |
| TSV ops headers | format=`tsv`, 6 geo-valid, `label`/`lng` mapped |
| JSON standard | both sides format=`json`, 6/6 rows |
| JSONL | format=`jsonl`, B count 6 |
| GeoJSON | format=`geojson`, 6 geo-valid, names from properties |
| UTF-8 BOM CSV | 6 geo-valid after `utf-8-sig` decode |
| Cross-format CSV+TSV | both sides ingest with correct formats |
| Empty A + valid B | 200, A=0, B=6 |
| Header-only A + valid B | 200, A=0 |
| Both empty / header-only alone | **400** |
| Malformed JSON | **400** with parse detail |

### Compare via API (`TestCompareViaApi`)

| Test | Expected |
|------|----------|
| `/compare/demo` | ≥1 temporal, ≥1 spatial |
| JSON→JSON known pairs | temporal (~17d), spatial (100–250 m, name&lt;0.75), exactish present; ≥3 pairs |
| CSV alias→TSV ops | classes include temporal + spatial |
| JSONL→GeoJSON | spatial pair found |
| BOM CSV→JSON | ≥3 pairs |
| Empty compare body | **400** |
| `/demo/ingest-preview` | preview length 15, geo_valid &gt; 0 |

### Contract (`TestScoreBreakdownContract`)

Match payloads include `geo/name/attr/temporal/composite` scores in \[0,1\], `pair_id`, `classification`.

### Prior unit coverage (still green)

- Engine temporal + spatial classification  
- Demo fixture class counts  
- Alias/regex mapping + sniff helpers  
- API health / presets / demo smoke  

## How to reproduce

```bash
python3 -m venv .venv && .venv/bin/pip install -e ".[test]"
.venv/bin/python scripts/generate_import_samples.py
PYTHONPATH=src .venv/bin/pytest -q
```

## Verdict

Import sniffing + column alias/regex mapping + compare classifications are robust across the sample matrix above. Safe to land to `main`.
